import { green } from "@mui/material/colors";
import { orange } from "@mui/material/colors";

const titleStyle = {
  fontSize: "19px",
  fontWeight: 600,
  ml: 1,
  mt: 0.6,
};
export { titleStyle };

const dateColor = green[500];
const deadlineColor = orange[500];
export { dateColor, deadlineColor };
